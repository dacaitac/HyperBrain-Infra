# Environment & secrets — NUNCA subir valores reales
.env
.env.*
!.env.example

# Secretos cifrados con SOPS+age SÍ pueden versionarse:
!secrets.enc.env

# SOPS / age keys
*.agekey
keys.txt

# Certificates & keys
*.pem
*.key
*.crt

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
*.iml
.vscode/

# Docker / runtime
**/data/
*.log

# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
terraform.tfvars

# Python bytecode (lambda handlers)
__pycache__/

# Lambda build artifacts (terraform archive_file)
**/.build/

# backups locales de hardening
docker-compose.yml.bak-*

# Claude Code — commit settings.json + hooks/; ignore local overrides (ADR-017)
.claude/settings.local.json
