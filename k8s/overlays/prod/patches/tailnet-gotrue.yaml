# Tailnet exposure (ADR-021 D2/F6'): `hyperbrain-gotrue` — auth datasource for
# HyperBrain-ios-App (ADR-024), read-models-only per ADR-022 D2.
apiVersion: v1
kind: Service
metadata:
  name: gotrue
  namespace: hyperbrain
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: hyperbrain-gotrue
