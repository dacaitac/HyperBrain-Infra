# PREPARED, NOT DEPLOYED (Sprint 3 #45). Tailnet exposure for the user via the
# Tailscale Kubernetes Operator when it lands — never a NodePort/hostPort.
apiVersion: v1
kind: Service
metadata:
  name: appsmith
  namespace: hyperbrain
  labels:
    app: appsmith
spec:
  selector:
    app: appsmith
  ports:
    - name: http
      port: 80
      targetPort: http
